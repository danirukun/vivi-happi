---
import { Image } from 'astro:assets';
import oshimoshiLogo from '../images/Oshimoshi-Logo.png';

const currentLocale = Astro.currentLocale ?? 'en';
const isJapanese = currentLocale === 'ja';
---

<section id="preorder" class="py-16 px-4 sm:px-6 lg:px-8">
	<div class="max-w-3xl mx-auto text-center text-vivi-purple-800">
		<h2 class="text-4xl font-bold font-yuruka mb-4">{isJapanese ? '予約受付は終了しました' : 'Preorders are Closed'}</h2>
		<p class="text-xl tracking-wide mb-8 opacity-90">{isJapanese ? 'たくさんのご注文ありがとうございました！' : 'Thank you for your overwhelming support!'}</p>
		<div
			class="inline-block px-8 py-4 bg-gray-400 text-vivi-pink-50 rounded-full font-bold text-lg cursor-not-allowed mb-8"
		>
			{isJapanese ? '予約終了' : 'Preorders Closed'}
		</div>
		<div class="mt-8">
			<a href="https://oshimoshi.shop/" target="_blank" rel="noopener noreferrer">
				<Image src={oshimoshiLogo} alt={isJapanese ? 'Oshi Moshi（販売パートナー）' : 'Oshi Moshi - Distribution Partner'} width={150} height={75} class="mx-auto" />
			</a>
		</div>
	</div>
</section>

