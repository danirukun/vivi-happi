---

export interface Product {
	name: string;
	description: string;
	variant: 'A' | 'B';
}

interface Props {
	description: string;
	products: Product[];
}

const { description = 'Something about the project', products = [] } = Astro.props;
---

<div class="max-w-3xl mx-auto text-center">
	<h2 class="text-4xl font-moco text-vivi-pink-600 mb-6">About the project</h2>
	<div class="space-y-4 text-lg text-gray-700">
		<p>{description}</p>
		<div class="grid grid-cols-1 md:grid-cols-3 gap-4 mt-8">
			{products.map((product) => (
				<div class={`p-4 bg-vivi-${product.variant === 'A' ? 'pink' : 'purple'}-50 rounded-lg`}>
					<p class={`font-semibold text-vivi-${product.variant === 'A' ? 'pink' : 'purple'}-700`}>{product.name}</p>
					<p class="text-sm text-gray-600 mt-1">{product.description}</p>
				</div>
			))}
		</div>
	</div>
</div>

